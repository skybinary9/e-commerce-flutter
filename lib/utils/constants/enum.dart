//Lists of enum 

enum TextSize {small, medium, large, }

enum OrderStatus {processing, shipped, delivered, cancelled, returned}

enum PaymentMethod {paypal, googleplay, applepaly, visa, mastecard, craditcard, paystack, razorpay, paytm}